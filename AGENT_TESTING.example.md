# Agent 使用者端全功能實測紀錄

> 這是單次實測工作的進度帳本。正式開始前，將 `AGENT_TESTING.example.md` 複製為受 Git 忽略的 `AGENT_TESTING.md`。實測從使用者已建立自己的範本副本後開始；README 的共用範本不是測試目標，也不因出現在文件中而取得操作授權。

## 使用規則

- `[ ]` 表示尚未完成；只有實際觀察到預期結果後才能改成 `[x]`。
- 失敗或受阻的項目保持 `[ ]`，並立刻寫入「阻塞與缺陷」及「操作紀錄」。
- 每次瀏覽器、Google Sheets、Apps Script 初始化、部署或 Web App 操作後，立即更新本檔；不得等整批測試結束才補寫。
- 恢復測試時先閱讀「目前進度」、「阻塞與缺陷」及最後一筆操作紀錄，從第一個未勾選項目繼續。
- 只操作本機 `.env` 明列的 `SPREADSHEET_ID` 與 `APPS_SCRIPT_ID`；ID 不完整或不一致時停止。
- 測試資料統一使用本檔記錄的前綴，避免與真實資料混淆。除非使用者另行授權，不刪除或覆寫非測試資料。
- 不在可提交檔案中記錄密碼、Session Token、OAuth Token、Script Properties secrets 或私人部署網址。
- 功能修正後，重新執行受影響步驟及其下游流程，不能只把原有項目重新勾選。

## 目前進度

- 狀態：未開始
- 最後完成階段：無
- 下一步：確認使用者已建立自己的試算表副本
- 開始時間（Asia/Taipei）：
- 最後更新時間（Asia/Taipei）：
- 執行 Agent／人員：
- 測試資料前綴：`AGENT-E2E-`

## 本次目標與環境

- Spreadsheet ID：從本機 `.env` 讀取，不在此範本填入真實值
- Apps Script ID：從本機 `.env` 讀取，不在此範本填入真實值
- Google 帳號：
- Web App deployment ID：
- Web App `/exec` URL：只填在受忽略的 `AGENT_TESTING.md`
- 瀏覽器／Profile：
- 桌面 viewport：
- 手機 viewport：360、375、390 px
- 測試帳密保管位置：只記錄位置，不在 example 中填入密碼

## 0. 副本完成後的安全檢查

- [ ] 使用者已從 README 的範本建立自己的 Google 試算表副本。
- [ ] 副本 ID 與 README 共用範本 ID 不同。
- [ ] 本機 `.env` 已填入副本的 `SPREADSHEET_ID`。
- [ ] 已從副本的 `擴充功能 → Apps Script` 取得其 `APPS_SCRIPT_ID` 並更新 `.env`。
- [ ] 已確認目前 Google 帳號對副本及 Apps Script 專案有擁有者或編輯權限。
- [ ] 已記錄測試開始前的 Sheet、Script Properties、deployment 與資料快照。

## 1. 首次初始化

- [ ] 從副本的綁定 Apps Script 編輯器選擇 `setupSystem()`。
- [ ] 首次執行時完成必要 Google 授權。
- [ ] `setupSystem()` 成功，回傳 `success: true`。
- [ ] 回傳的 `spreadsheetId` 與目前副本 ID 相同。
- [ ] 回傳的 `spreadsheetUrl` 指向目前副本，而不是 README 範本。
- [ ] Script Properties 自動保存正確的 `SPREADSHEET_ID`。
- [ ] Script Properties 已建立 `PASSWORD_PEPPER`、`SESSION_SECRET` 與 `SYSTEM_INITIALIZED=true`，但未把值抄入本檔。
- [ ] 首次管理員帳號及臨時密碼已安全保存，明碼未出現在 Sheet。
- [ ] 副本中存在 `students`、`users`、`classes`、`score_reasons`、`scores`、`attendance`、`settings`、`audit_logs`、`sessions` 共 9 張必要 Sheet。
- [ ] 9 張 Sheet 的 Header、freeze、filter、欄寬、日期／時間／Boolean 格式正確。
- [ ] 預設 settings 完整且時區為 `Asia/Taipei`。
- [ ] 第二次執行 `setupSystem()` 成功，沒有重複 Sheet、Header、設定或管理員。
- [ ] 第二次執行後既有測試資料仍保留。
- [ ] Google Drive 中沒有因初始化意外產生另一份「班級管理系統資料庫」。

## 2. Web App 部署與入口

- [ ] 建立 `Deploy → New deployment → Web app`。
- [ ] `Execute as` 設為部署者。
- [ ] 存取範圍符合目前 Workspace／組織政策與測試需求。
- [ ] deployment ID 與 `/exec` URL 已記入本機 `AGENT_TESTING.md`。
- [ ] 以無應用程式 Session 的瀏覽器開啟 `/exec`，登入頁可正常載入。
- [ ] Bootstrap CSS、Bootstrap Bundle 與 Bootstrap Icons 均成功載入。
- [ ] 頁面沒有未處理 JavaScript 錯誤、CSP 錯誤或失敗的 GAS 呼叫。
- [ ] 未登入時不能看到或操作管理畫面。

## 3. 首次管理員與登入流程

- [ ] 錯誤帳號或密碼會顯示通用錯誤，且不洩漏帳號是否存在。
- [ ] 首次管理員可以臨時密碼登入。
- [ ] 登入後顯示正確姓名、角色與可用導覽。
- [ ] Dashboard 在尚無測試資料時顯示合理 empty state／零值。
- [ ] 修改密碼時，錯誤舊密碼會失敗且原密碼仍有效。
- [ ] 修改為有效新密碼成功。
- [ ] 登出後回到登入頁，舊 Session 不再可用。
- [ ] 可以使用新密碼重新登入。
- [ ] 重新整理頁面時有效 Session 可恢復；過期或失效 Session 會回到登入頁。

## 4. 建立基準測試資料

- [ ] 以測試前綴建立兩個班級，涵蓋不同年級或學期。
- [ ] 每個班級至少建立兩名學生，學號與座號皆不同。
- [ ] 建立一個正向加分原因及一個負向扣分原因。
- [ ] 建立導師、任課教師、行政與第二管理員測試帳號。
- [ ] 導師與任課教師分別綁定指定測試班級。
- [ ] 記錄所有測試資料 ID 或可識別名稱，供後續驗證與清理。
- [ ] 重複學號、重複帳號及其他唯一值衝突均被阻擋，且 Sheet 沒有新增髒資料。

## 5. 班級管理

- [ ] 管理員可查詢、新增、修改及停用／啟用班級。
- [ ] 班級列表正確顯示學年度、學期、年級、班名、導師及狀態。
- [ ] 停用班級後，需 active class 的操作不再提供該班級。
- [ ] 重新啟用班級後可恢復操作。
- [ ] 無效學年度、學期、年級或缺少必填欄位會被後端拒絕。
- [ ] 所有 CREATE／UPDATE／狀態變更均寫入 Audit Log。

## 6. 學生管理與個人資料

- [ ] 管理員可依班級、關鍵字與啟用狀態查詢學生。
- [ ] 管理員可新增、修改及停用／啟用學生。
- [ ] 學生列表的學號、姓名、班級、座號及狀態正確。
- [ ] 停用學生不會出現在需 active student 的點名名單。
- [ ] 學生詳細資料可以開啟。
- [ ] 詳細資料中的累計分數、加減分歷史及出缺席歷史與 Sheet 相符。
- [ ] 修改學生班級或座號後，列表及詳細資料同步更新。
- [ ] 無效班級、重複學號、無效座號或缺少必填欄位會被拒絕。
- [ ] 所有 CREATE／UPDATE／狀態變更均寫入 Audit Log。

## 7. 加減分規則與紀錄

- [ ] 管理員可新增、修改及停用／啟用加減分原因。
- [ ] 正向原因、負向原因、預設分數、排序與說明顯示正確。
- [ ] 導師或任課教師可對授權班級學生新增加減分。
- [ ] 加減分紀錄可依日期、班級或學生正確查詢。
- [ ] 修改既有分數、原因或備註後 UI 與 Sheet 一致。
- [ ] 刪除加減分紀錄使用確認 Modal，不使用原生 `confirm()`。
- [ ] 刪除後紀錄為 Soft Delete，不再出現在有效列表，但原始列仍保留。
- [ ] 停用原因不再提供給新紀錄使用，既有歷史仍可辨識。
- [ ] 無效日期、學生、原因、分數或越權班級會被後端拒絕。
- [ ] CREATE／UPDATE／DELETE 均寫入 Audit Log。

## 8. 每日點名

- [ ] 選擇日期與班級後，只載入該班級啟用中的學生。
- [ ] 每位學生可選出席、遲到、早退、缺席、請假。
- [ ] 選擇請假時會顯示假別；切回非請假狀態後假別被清空。
- [ ] 到校時間、離校時間及備註可以儲存並重新載入。
- [ ] 「全部出席」可正確更新全班。
- [ ] 批次儲存後重新載入，所有狀態與 Sheet 一致。
- [ ] 同一日期與學生重複儲存使用 Upsert，不新增第二筆有效紀錄。
- [ ] 刪除點名紀錄為 Soft Delete，重新登錄後仍只有一筆有效紀錄。
- [ ] 無效日期、時間、狀態、假別、學生或越權班級會被後端拒絕。
- [ ] CREATE／UPDATE／DELETE 均寫入 Audit Log。

## 9. Dashboard 與學生歷史

- [ ] Dashboard 學生總數與測試班級啟用學生數一致。
- [ ] 出席、遲到、早退、缺席、請假統計與指定日期點名資料一致。
- [ ] 本日加分、扣分及淨分數與加減分紀錄一致。
- [ ] 切換班級或日期後所有統計同步更新。
- [ ] 點名完成度計算正確。
- [ ] 學生詳細資料中的正分、負分、總分與 Dashboard／Sheet 一致。
- [ ] 加減分與出缺席歷史按合理順序顯示，Soft Delete 紀錄不出現。

## 10. 使用者、角色與授權

### 管理員

- [ ] 管理員看得到全部功能，能管理所有班級與系統設定。
- [ ] 管理員可新增、修改、停用／啟用使用者及重設密碼。
- [ ] 停用使用者或重設密碼後，該使用者既有 Session 立即失效。

### 導師

- [ ] 導師只看到 Dashboard、今日點名、學生管理與加減分等適用功能。
- [ ] 導師可讀寫自己授權班級的學生、點名與加減分。
- [ ] 導師看不到其他班級資料。
- [ ] 導師對其他班級的直接 server call 會收到權限錯誤且不寫入資料。

### 任課教師

- [ ] 任課教師只看到 Dashboard、學生查詢與加減分等適用功能。
- [ ] 任課教師可查看授權班級並新增／修改加減分。
- [ ] 任課教師不能進行點名或學生管理寫入。
- [ ] 任課教師對未授權班級或 Attendance API 的直接呼叫會失敗且不寫入資料。

### 行政

- [ ] 行政可查看全部學生與班級並管理 Attendance。
- [ ] 行政不能管理加減分、使用者或系統設定。
- [ ] 行政對 Scores、Users 或 Settings 寫入 API 的直接呼叫會失敗且不寫入資料。

### 共通授權

- [ ] 四種角色的導覽顯示與後端權限結果一致，不能只靠隱藏選單。
- [ ] 停用帳號無法登入。
- [ ] 未登入、空 Token、偽造 Token 與過期 Token 均無法呼叫受保護 API。

## 11. 系統設定

- [ ] 管理員可讀取並更新 system name、school name、school year、semester、late time、session TTL 與 timezone。
- [ ] 更新後重新整理頁面，公開系統名稱及學校名稱正確。
- [ ] 數字、時間及必填設定的無效輸入會被拒絕。
- [ ] 非管理員不能查看或修改管理設定。
- [ ] 設定修改寫入 Audit Log。
- [ ] 測試結束前可將所有修改過的設定恢復原值。

## 12. Audit Log

- [ ] LOGIN、LOGOUT、PASSWORD_CHANGE 均有正確稽核紀錄。
- [ ] Classes、Students、Users、Score Reasons、Scores、Attendance、Settings 的 CREATE／UPDATE／DELETE 或狀態變更均有紀錄。
- [ ] 稽核紀錄包含使用者、操作、資料表、資料 ID、修改前後、來源與時間。
- [ ] 稽核紀錄沒有宣稱或偽造使用者真實 IP。
- [ ] 桌面版以 Table 顯示，手機版以 Accordion 顯示。
- [ ] 大型 JSON 預設不會直接鋪滿主畫面。

## 13. Session 與安全行為

- [ ] 連續活動會更新 Session 最後活動時間。
- [ ] 登出、停用帳號、管理員重設密碼及使用者修改密碼會依規格使 Session 失效。
- [ ] 以可安全還原的短 TTL 測試 Session 到期，過期後 UI 清除 Token 並回登入頁。
- [ ] 密碼 Sheet 欄位只出現 `salt$hash`，沒有明碼。
- [ ] HTML、前端 JavaScript、網路回應及 localStorage 不包含 pepper、session secret 或密碼雜湊。
- [ ] localStorage 只保存預期的 Session Token 與顯示／主題偏好。
- [ ] 使用含 HTML／Script 字元的測試名稱與備註時，畫面只顯示文字，不執行內容。
- [ ] 危險操作都有確認 Modal，取消後不改變資料。
- [ ] Loading、錯誤 Toast、Session expired 流程及重複點擊防護正常。

## 14. Responsive、主題與無障礙

### 桌面與平板

- [ ] 桌面 viewport 操作所有頁面，Table、Toolbar、Modal 與 Sidebar 無重疊或裁切。
- [ ] 平板 viewport 操作所有頁面，導覽與內容可用。
- [ ] 大量測試資料下 Table 可讀，操作按鈕不溢出。

### 手機 360／375／390 px

- [ ] 分別在 360、375、390 px 開啟並操作所有角色可見頁面。
- [ ] 手機主選單使用 Offcanvas，開啟、導覽與關閉正常。
- [ ] 今日點名使用學生 Card，五種狀態、假別及時間欄位可操作。
- [ ] 學生列表與學生歷史使用 Card，不出現不可操作的大型 Table。
- [ ] Audit Log 使用 Accordion。
- [ ] Modal、確認視窗、Toast、表單及篩選器不超出 viewport。

### 顯示模式與主題

- [ ] 自動／手機／電腦顯示模式皆可切換。
- [ ] 強制電腦模式在窄螢幕仍可操作，不造成不可恢復的水平裁切。
- [ ] 自動／亮色／深色主題皆可切換。
- [ ] 重新整理後顯示模式與主題偏好保留。
- [ ] 系統主題變更時，自動模式會跟隨。
- [ ] `prefers-reduced-motion` 下沒有不必要動畫。

### 鍵盤與輔助技術

- [ ] 只用鍵盤可登入、導覽、操作表單、Modal、Offcanvas 與主要 CRUD。
- [ ] Focus 順序合理且 Focus indicator 可見。
- [ ] Icon-only 控制具有可讀名稱；裝飾 icon 不造成噪音。
- [ ] 表單 label、錯誤、狀態與 Modal 名稱可由螢幕閱讀器理解。
- [ ] 亮色與深色模式文字、狀態及互動控制具足夠對比。

## 15. 併發、Lock 與資料一致性

- [ ] 使用兩個獨立瀏覽器 Session／Profile 登入可操作同班級的授權帳號。
- [ ] 兩邊同時儲存同日同學生點名，最後只有一筆有效 Attendance。
- [ ] 兩邊同時新增或修改分數，不產生破損列或部分寫入。
- [ ] Script Lock timeout 或衝突會顯示可理解錯誤，不造成重複提交。
- [ ] 重新載入後 UI、Sheet 與 Audit Log 的最終資料一致。

## 16. 錯誤處理與恢復

- [ ] 空資料、查無結果及停用資料都有合理 Empty State。
- [ ] 網路或 GAS 呼叫失敗時 Loading 會解除，使用者可重試。
- [ ] 表單驗證失敗不會關閉到無法修正，也不會留下部分資料。
- [ ] Session 到期時目前畫面安全退出，不殘留可操作的敏感內容。
- [ ] 重新整理、返回、Modal 取消及重複操作不會綁定重複事件。
- [ ] 修正一個缺陷後，相關流程可從本檔記錄的位置繼續並重測。

## 17. 測試資料清理與最終確認

- [ ] 已保存需要保留的缺陷證據與重現步驟。
- [ ] 已將測試期間調整的 settings 恢復原值。
- [ ] 已停用或依產品規則清理所有測試使用者、班級、學生及加減分原因。
- [ ] 已 Soft Delete 不需保留的測試 Scores 與 Attendance，未直接刪除 Sheet 列。
- [ ] 清理後 Dashboard、有效資料列表與 Sheet 一致。
- [ ] 已確認沒有操作 README 共用範本或其他非 `.env` 目標。
- [ ] 已將實機驗證結果同步回 `ACCEPTANCE.md`；只勾選有實際證據的項目。
- [ ] 「阻塞與缺陷」中的每一項都有結果、後續行動或明確保留原因。
- [ ] 所有必要項目完成，或已由使用者接受剩餘風險。
- [ ] 最終測試摘要已寫入本檔並回報使用者。

## 阻塞與缺陷

| ID | 發現時間 | 階段／項目 | 嚴重度 | 狀態 | 重現步驟與觀察 | 預期結果 | 後續行動 |
|---|---|---|---|---|---|---|---|
| — | — | — | — | — | 尚無 | — | — |

## 操作紀錄

每次遠端或瀏覽器操作後追加一列。不要記錄密碼、Token 或 secrets。

| 時間（Asia/Taipei） | 階段／項目 | 實際操作 | 結果 | 證據／備註 |
|---|---|---|---|---|
| — | — | 尚未開始 | — | — |

## 最終摘要

- 完成時間：
- 通過項目：
- 失敗項目：
- 未執行項目：
- 已知風險：
- 建立或更新的 deployment：
- 建議下一步：
